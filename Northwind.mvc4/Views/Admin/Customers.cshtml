@model IEnumerable<AppCore.Customer.Customer>
@{
    ViewBag.Title = "Customers";
}

<div class="row">
    <div class='col-sm-12'>
        <ul class="breadcrumb">
            <li><a href="/">Home</a></li>
            <li><a href="/admin">Administration</a></li>
            <li class="active">Customers</li>
        </ul>
    </div>
</div>

<div class="row">
    <div class="col-sm-12">
        <div class="well">
            @using (Html.BeginForm())
            {
                if (!String.IsNullOrEmpty(ViewBag.Message))
                {
                    <div class='alert alert-success' role='alert'>
                        <span class='glyphicon glyphicon-ok' aria-hidden='true'></span>
                        <span class='sr-only'>Success: </span>
                        @ViewBag.Message
                    </div>
                }
                if (!Html.ViewData.ModelState.IsValid)
                {
                    <div class='alert alert-danger' role='alert'>
                        <span class='glyphicon glyphicon-exclamation-sign' aria-hidden='true'></span>
                        <span class='sr-only'>Error:</span>
                        @Html.ValidationSummary("")
                    </div>
                }

                <div style="display:table;width:100%;margin-top:15px;">
                    <div style="display:table-row;">
                        <div style="display:table-cell;width:100%;">
                            <input id="searchterm" name="searchterm" type="text" placeholder="search for customer" class="form-control"
                                   style="width:100%;max-width:none;" value='@Request["searchterm"]' />
                        </div>
                        <div style="display:table-cell;white-space:nowrap;font-size:1px;">
                            <input type="submit" class="btn btn-default" style="margin-bottom:10px;" value="Search" />
                            <a href="/Admin/CustomerAdd" class="btn btn-default" style="margin-bottom:10px;">Add</a>
                        </div>
                    </div>
                </div>
            }
            <table class="table demo table-bordered">
                <thead>
                    <tr>
                        <th data-hide="">Name</th>
                        <th data-hide="phone">Contact</th>
                        <th data-hide="phone">Title</th>
                        <th data-hide="all">Address</th>
                        <th data-hide="phone">City</th>
                        <th data-hide="all">Region</th>
                        <th data-hide="phone">Country</th>
                        <th data-hide="all">Postal</th>
                        <th data-hide="tablet,phone">Phone</th>
                        <th data-hide="all">Fax</th>
                        <th data-hide="phone"></th>
                    </tr>
                </thead>
                <tbody>
                    @{
                        int i = 0;
                        foreach (var item in Model)
                        {
                            <tr>
                                <td>@Html.DisplayFor(model => item.CompanyName)</td>
                                <td>@Html.DisplayFor(model => item.ContactName)</td>
                                <td>@Html.DisplayFor(model => item.ContactTitle)</td>
                                <td>@Html.DisplayFor(model => item.Address)</td>
                                <td>@Html.DisplayFor(model => item.City)</td>
                                <td>@Html.DisplayFor(model => item.Region)</td>
                                <td>@Html.DisplayFor(model => item.Country)</td>
                                <td>@Html.DisplayFor(model => item.PostalCode)</td>
                                <td>@Html.DisplayFor(model => item.Phone)</td>
                                <td>@Html.DisplayFor(model => item.Fax)</td>

                                <!-- Commands-->
                                <td class="col-md-2">
                                    <a href='/admin/customeredit/@item.CustomerID' class='button special'>Edit</a>
                                    <a href='/admin/customerdelete/@item.CustomerID' class='button special'>Delete</a>
                                </td>

                            </tr>
                        }
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

@section Scripts
{
    @Styles.Render("~/Scripts/footable-v2/css/footable.standalone.css")
    @Scripts.Render("~/Scripts/footable-v2/js/footable.js")

    <style type="text/css">
    </style>
    <script type="text/javascript">
        jQuery(function ($) {
            $('.table').footable();

            $('.table').bind('footable_breakpoint', function () {
                $('.table').trigger('footable_expand_first_row');
            });
        });
    </script>
}














